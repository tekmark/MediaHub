<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    tools:context="com.example.chao.mediahub.SearchBarFragment">

    <!-- TODO: Update blank fragment layout -->
    <Button
        android:id="@+id/search_bar_button_search"
        android:text="Search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"/>
    <EditText
        android:layout_toLeftOf="@id/search_bar_button_search"
        android:layout_toStartOf="@id/search_bar_button_search"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignBottom="@id/search_bar_button_search"
        android:layout_alignTop="@id/search_bar_button_search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:singleLine="true"
        android:text="@string/hello_blank_fragment"/>
</RelativeLayout>
